// STUB: port from /offerhound-repo/src/components/BetaThankYouDialog.tsx — separate session task.
import React from 'react';
import { View } from 'react-native';

interface Props {
  isOpen: boolean;
  onClose: () => void;
  feedbackTitle: string;
}

export const BetaThankYouDialog: React.FC<Props> = (_props) => {
  return <View />;
};

export default BetaThankYouDialog;
