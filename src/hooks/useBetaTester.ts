// STUB: port from /offerhound-repo/src/hooks/useBetaTester.ts — separate session task.
// Shape matches Lovable hook exactly so consumers type-check cleanly.
export const useBetaTester = () => {
  return { isBetaTester: false as boolean, isLoading: false as boolean };
};
